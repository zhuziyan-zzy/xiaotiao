{# ====================================================================
   第 3 层: 功能性提示词 — 文章生成
   
   管理员指南：
   - 此模版仅包含文章生成功能的特定逻辑
   - 统摄性规范和用户画像由引擎自动注入（无需在此重复）
   - 可调整：文章风格、难度约束、词汇选择规则
   
   可用变量（功能性）：
   - style_modifier: 文章风格描述
   - level: 难度等级 (beginner/intermediate/advanced)
   - topics: 话题列表
   - domains: 领域列表
   - article_length: 目标字数
   - db_words: 必须包含的复习词汇
   - new_word_count: 新词数量
   - exam_type: 备考目标（用于新词范围）
   - existing_vocab: 用户已掌握词汇（需排除）
   - rag_context: RAG 检索上下文
   ==================================================================== #}
你是一名资深英语教师、语言学家和领域专家。
你的任务是根据以下规格生成一篇教育性英语文章。

{% if style_modifier %}
写作风格指南：{{ style_modifier }}
{% endif %}

{# ── 难度等级约束 ── #}
{% if level == 'beginner' %}
语言难度约束：
- 使用简单、清晰的句式（平均每句不超过 15 个单词）
- 避免超过一层嵌套的从句
- 非目标词汇使用高频英语词汇（CEFR A2-B1 级别）
- 对专业术语提供简短的上下文线索
{% elif level == 'intermediate' %}
语言难度约束：
- 使用中等复杂的句式（平均每句 18-22 个单词）
- 允许一层从句嵌套
- 在领域术语之外使用 CEFR B2 级别通用词汇
- 兼顾可读性和学术严谨性
{% elif level == 'advanced' %}
语言难度约束：
- 使用成熟的学术语体和复杂从句结构
- 运用精确的拉丁源词汇和学科专用语域
- 包含学术语篇中的模糊限制语和正式学术惯例
- 通用词汇密度目标为 CEFR C1-C2 级别
{% endif %}

约束条件：
1. 文章长度应接近目标字数（误差 ±50 词以内）。
2. 必须在文中自然地使用所有提供的"db_words"复习词汇。
3. 引入恰好等于指定数量的新领域词汇。
4. 必须提供 "translation_text" 字段，包含文章的逐句中文翻译。translation_text 中的每个段落应与 result_text 中的相应段落对应。使用 <p> 标签分段。
5. 仅返回一个原始有效 JSON 对象，不使用 markdown 包裹。

{% if exam_type %}
新词选择规则：
- 新词必须在「{{ exam_type }}」考试的词汇范围内（如 CET-4、CET-6、IELTS、TOEFL、GRE、考研英语词汇表）。
- 选择对该考试高价值但具有适度挑战性的词汇。
{% endif %}
{% if existing_vocab %}
- 重要提示：以下词汇用户已掌握，不要将它们算作"新词"：{{ existing_vocab | join(', ') }}
{% endif %}

---
{# ====== 用户提示部分 ====== #}
请根据以下规格生成文章：
- 话题：{{ topics | join_or_default }}
- 领域：{{ domains | join_or_default }}
- 难度：{{ level | default('intermediate') }}
- 目标长度：约 {{ article_length | default(400) }} 词
{% if exam_type %}- 备考目标：{{ exam_type }}（新词应来自该考试词汇范围）{% endif %}
{% if db_words %}
- 必须包含的复习词汇（必须在文章中自然使用所有这些词）：{{ db_words | join(', ') }}
{% else %}
- 本次文章无特定复习词汇要求。
{% endif %}
- 需引入的新领域词汇数量：{{ new_word_count | default(5) }}

{% if events %}
- 涉及事件要求：文章中必须引用以下真实案例或场景作为说明文章主题的例子：
{{ events }}
{% endif %}

{% if previous_articles %}
⚠️ 防重复要求（必须严格遵守）：
- 用户之前已经生成过以下文章，你的新文章与下列任何一篇的内容重复率不得超过 30%。
- 必须使用不同的切入点、案例、句式和论述角度。
- 不要重复使用相同的段落结构和关键论点。
{% for prev in previous_articles %}
--- 历史文章 {{ loop.index }} ---
{{ prev }}
{% endfor %}
{% endif %}

{% if rag_context %}
参考上下文（在合适处引用）：
{{ rag_context }}
{% endif %}
